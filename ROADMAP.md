# ROADMAP

这份文件回答两个问题：**这个工具要帮用户做什么**（use case），以及**先做哪个**（story map）。

用户画像：个人用户，用多个 Coding Agent 协作开发这个项目；自己也用 Codex / Claude / Cursor 做别的项目。

## 一、Use Case 总览

### UC1 定义智能体

把"我想要一个什么样的智能体"说清楚，说到能验收的程度。

- UC1.1 填写要件定义：目的、服务对象、范围、输入契约、动作契约、输出契约、验收条件、失败处理。
- UC1.2 配置成员脚手架：输入处理、Guardrails、Grounding、指令与技能、上下文与记忆、模型、工具、执行循环、输出与补救（9 个配置区）。
- UC1.3 管理配置版本：保存草稿、生成不可变快照、设为默认版本是三个独立操作；误关闭/误切换时未保存草稿不丢。
- UC1.4 从模板或已有配置开始：从单智能体/团队模板创建；复制一个已调好的成员再修改，而不是每次从零填 9 个配置区。

### UC2 编排团队

把多个智能体连成一个团队，定义它们怎么交接。

- UC2.1 在自由画布上拖拽节点、连线、配置分支与并行汇合。
- UC2.2 定义连线上的字段映射与交接条件（委派 vs 交接控制权）。
- UC2.3 区分设计结构与某次执行的实际路径两种视图。

### UC3 试运行与观察

跑一次任务，看清楚每一步发生了什么。

- UC3.1 输入任务或选择测试案例，查看流式过程、当前步骤、待批准操作、预算消耗。
- UC3.2 处理人工确认节点：批准或拒绝。
- UC3.3 遇到失败、取消、超时、预算耗尽、循环达上限时，状态明确可见。

### UC4 追踪与定位问题（Trace）

从一次运行定位到具体哪一步出了问题。

- UC4.1 按"团队运行 → 成员调用 → 检索/模型/工具/交接/校验"展开执行树。
- UC4.2 查看每条记录的实际输入输出、耗时、状态、模型、Token、成本、配置版本。
- UC4.3 从失败步骤直接跳回对应的成员配置。
- UC4.4 从失败步骤一键创建修复草稿：带着当时的配置快照生成一个可修改的候选版本，进入 UC6 的比较流程。

### UC5 评估质量（Judge）

执行完成不等于合格，单独评价质量。

- UC5.1 Code-as-a-Judge：格式、必填字段、动作凭证等确定性检查。
- UC5.2 LLM-as-a-Judge：完成度、依据充分性、相关性，含评分标准与理由。
- UC5.3 人工评价：处理争议与机器无法判断的结果，记录与机器评分的分歧。

### UC6 回归与版本比较

改完配置后，用同一批测试验证是变好了还是变差了。

- UC6.1 从失败步骤一键转为回归用例，补充预期条件。
- UC6.2 固定数据集版本和评估器版本，比较基线与候选。
- UC6.3 查看改善/退化/无法判定及耗时成本变化，决定是否把候选设为默认版本。
- UC6.4 修改配置后试运行时，系统明确提示评估成绩缺失或不可比（不套用预置版本的成绩），让用户对"这次模拟没有质量结论"有正确预期。

### UC7 管理资产与数据

- UC7.1 资源库：管理可复用的工具、技能、知识源条目。
- UC7.2 本地备份：JSON 导出/恢复配置（当前数据在浏览器 localStorage）。
- UC7.3 为 Coding Agent 生成实现用的 UI/智能体 Prompt。
- UC7.4 数据风险应对：存储接近上限、数据损坏、换浏览器/端口后的找回引导（损坏时保留原始数据供导出）。

## 认领机制

Story Map 第二行（走得深）是当前施工区。认领与分工规则见 `AGENTS.md` 的"认领机制"一节。

## 二、Story Map

横向是用户完成任务的主流程（Backbone），纵向是优先级：上面是"走过场"（能演示闭环），下面是"走得深"。

| 优先级 | 定义智能体 | 编排团队 | 试运行观察 | Trace 定位 | 评估质量 | 回归比较 | 资产管理 |
|---|---|---|---|---|---|---|---|
| **走过场（已实现，模拟数据）** | 要件表单、9 区脚手架、版本快照 | 拖拽连线、字段映射、双视图切换 | 模拟播放、人工批准、预算/超时状态 | 执行树、输入输出、跳回配置 | Code/LLM 模拟评价、人工批注 | 转回归用例、固定数据集比较、设默认版本 | 资源目录、JSON 备份 |
| **走得深（下一步）** | 要件关联测试用例 | 并行/工具节点的执行语义、子流程折叠 | **（最高优先）** 自定义输入也能得到完整的模拟轨迹 | 失败步骤一键生成修复草稿的完整闭环 | 评估器与评估器版本的选择规则 | 改善/退化的统计口径说明 | Prompt 导出内容与格式验收标准 |
| **接真实（更远）** | — | — | 接入真实执行引擎（配置快照不可变为前提） | OTLP/OTel 接入，记录适配器版本；从 Trace 空白处看出"未采集"原因 | 真实 LLM Judge、批量评测执行器（预留 Promptfoo 接口） | 真实数据集管理 | — |

读法：每一列先做完"走过场"再往下。当前整个产品处于第一行已完成、第二行进行中的状态。

## 三、接入真实能力的设计前提（Next 阶段动工前必须满足）

1. 配置快照不可变：运行永远绑定当时的配置版本，改配置产生新版本。
2. Trace 格式先行：先定内部 Trace 数据模型，再让真实引擎往里面填，UI 不需要大改。
3. 模拟与真实并存：每个模块能明确切换/标注数据来源，不允许混合显示却不说明。

## 四、明确不做

- 多用户、权限、多租户
- 计费与配额管理
- 生产部署与运维能力
- 后台常驻、开机启动（这个工具只在本机、用户明确要求时运行）
